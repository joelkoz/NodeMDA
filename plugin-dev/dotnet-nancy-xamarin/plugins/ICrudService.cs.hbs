##output overwrite {{@root.output}}/{{@root.libraryNamespace}}/ICrudService.cs

namespace {{@root.libraryNamespace}}
{
    /**
     * Synchronous access to a Crud service
     */
    public interface ICrudService<T>
    {
        T Create(T entity);
        T Update(T entity);
        bool Delete(int id);
        T Find(int id, bool loadRelated = true);
        T[] FindAll();
    }
}
