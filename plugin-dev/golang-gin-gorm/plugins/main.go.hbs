##output preserve {{@root.goProjectPath}}/main.go
package main

import (
   "{{@root.goProjectPath}}/app"

{{#each mainImport as |pkgImport|}}
   _ "{{pkgImport}}"
{{/each}}
)


func main() {
	// Set the REAL database name...
	app.DatabaseName = "/tmp/{{model.name}}.db"

	// Open the database and prepare for use
	db := app.GetDb()
	defer db.Close()


}
