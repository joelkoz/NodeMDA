{{#with class as |class|}}
##output {{#if class.hasOperations}}preserve{{else}}ignore{{/if}} {{class.goPackagePath}}/{{class.goFileName}}impl.go
package {{class.goPackageName}}


{{#each class.operations as |op|}}
{{#each op.goCommentsFormatted as |cmt|}}
// {{cmt}}
{{/each}}
func (entity *{{class.goName}}) {{op.goName}}({{#each op.parameters as |param|}}{{#unless @first}}, {{/unless}}{{param.goName}} {{param.goTypeName}}{{/each}}) {{op.goReturnType}} {
	panic("{{class.goName}}.{{op.goName}} not yet implemented");
}
    
{{/each}}	
{{/with}}
