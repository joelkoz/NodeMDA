##output preserve {{@root.output}}/src/services/{{class.packageDirName}}/{{class.name}}Dao/hooks/custom-hooks.js
'use strict';

// Custom hook functions for the {{class.name}} object...

module.exports = {

  hook1 : function(options) {

	  return function (hook) {
	      // this = the service object running the hook
	      // this.Model = MongooseDB model of the entity
	      // hook.method = The method name called (e.g. "find", "create", etc.)
	      // hook.type = The type of hook being called ("before" or "after") 
	      // hook.app = the Feathers/Express application object
	      // hook.params = the parameters passed to the service method
	      // hook.params.user = the user currently logged in
	      // hook.data = the data to be used for a "create, update, or patch" call
	      // hook.result = any result set returned from a service call (only available on AFTER hooks)
	  };
	},

};
